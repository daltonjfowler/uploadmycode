{
  "name": "uno-web-ide",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser Arduino IDE for district Chromebooks. Arduino Uno only.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "wrangler": "4.127.1"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7"
  }
}
